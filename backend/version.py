"""Single source of truth for the app version.

Stamped by packaging/set-version.sh across this file, ui/pubspec.yaml,
nfpm.yaml and the MSIX manifest so every artifact reports the same version.
"""

__version__ = "0.2.0"
